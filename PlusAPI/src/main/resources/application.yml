spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: geungan9@gmail.com
    password: fkxutsddgdecradp
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true
          timeout: 5000
  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    show-sql: true

logging:
  level:
    org:
      hibernate:
        sql: debug
---
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:test;MODE=MYSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

---
spring:
  config:
    activate:
      on-profile: test-mysql
  datasource:
    url: "jdbc:mysql://127.0.0.3:3306/english_test"
    username: "hwang"
    password: "5510"
  jpa:
    hibernate:
      ddl-auto: create
    database: mysql

---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: "jdbc:mysql://127.0.0.3:3306/english"
    username: "hwang"
    password: "5510"
  kafka:
    bootstrap-servers: 127.0.0.7:9092
server:
  port: 5510
logging:
  config: classpath:logback-local.xml
---
spring:
  config:
    activate:
      on-profile: server
  datasource:
    url: "jdbc:mysql://10.0.2.3:3306/english"
    username: "hwang"
    password: "5510"
  kafka:
    bootstrap-servers: 10.0.2.7:9092
server:
  port: 5511
logging:
  config: classpath:logback-server.xml

#  이제 local별 server별 servers 다르게 해서 진행하면되는 것이고 127.0.0.7:9092 10.0.2.7:하면 되는 것이다.
